%==============================================================================
%  Formal Proof: The Torsional Trefoil Markov Pump Theorem (TTMPT)
%  Owl Academy -- BioChain AI
%
%  Build:  tectonic TTMPT-Formal-Proof.tex
%      or  pdflatex TTMPT-Formal-Proof.tex   (run twice for headers/refs)
%==============================================================================
\documentclass[11pt]{article}

% --- Page geometry --------------------------------------------------------
\usepackage[letterpaper,margin=1in]{geometry}

% --- Fonts & encoding -----------------------------------------------------
\usepackage[T1]{fontenc}
\usepackage{mathpazo}      % Palatino text + matching math for a clean look
\usepackage{microtype}

% --- Mathematics ----------------------------------------------------------
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{amsthm}
\usepackage{mathtools}

% --- Layout & links -------------------------------------------------------
\usepackage{enumitem}
\usepackage{xcolor}
\usepackage{titlesec}
\usepackage{fancyhdr}
\usepackage[hidelinks]{hyperref}

% --- Accent colour (a muted Owl-Academy gold) -----------------------------
\definecolor{accent}{HTML}{8A6D1F}

% --- Section styling ------------------------------------------------------
\titleformat{\section}
  {\normalfont\large\bfseries\color{accent}}{\thesection}{0.75em}{}
\titleformat{\subsection}
  {\normalfont\normalsize\bfseries}{\thesubsection}{0.6em}{}

% --- Running headers ------------------------------------------------------
\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\small\itshape Torsional Trefoil Markov Pump Theorem}
\fancyhead[R]{\small\thepage}
\renewcommand{\headrulewidth}{0.4pt}

% --- PDF metadata ---------------------------------------------------------
\hypersetup{
  pdftitle={Formal Proof: The Torsional Trefoil Markov Pump Theorem (TTMPT)},
  pdfauthor={Owl Academy -- BioChain AI},
  pdfsubject={Stochastic pumps, broken detailed balance, Markov chains}
}

% --- Theorem environments -------------------------------------------------
\theoremstyle{plain}
\newtheorem{theorem}{Theorem}[section]
\newtheorem{lemma}{Lemma}[section]

\theoremstyle{definition}
\newtheorem{definition}{Definition}[section]
\newtheorem*{xdefinition}{Definition}

% --- Convenience macros ---------------------------------------------------
\newcommand{\Hilb}{\mathcal{H}}
\newcommand{\ket}[1]{\lvert #1 \rangle}
\newcommand{\bra}[1]{\langle #1 \rvert}
\newcommand{\braket}[2]{\langle #1 \,\vert\, #2 \rangle}
\newcommand{\norm}[1]{\lVert #1 \rVert}
\newcommand{\abs}[1]{\lvert #1 \rvert}
\newcommand{\Efwd}{E^{+}}

\allowdisplaybreaks

%==============================================================================
\begin{document}

\title{\bfseries Formal Proof:\\[2pt]
       The Torsional Trefoil Markov Pump Theorem (TTMPT)}
\author{Owl Academy \\[2pt] \normalsize BioChain AI}
\date{June 19, 2026}
\maketitle
\thispagestyle{fancy}

%------------------------------------------------------------------ Abstract
\begin{abstract}
\noindent
The Torsional Trefoil Markov Pump Theorem (TTMPT) establishes a deterministic
mathematical framework for a one-way stochastic pump mechanism acting on a
longitudinal binding-scalar field. This paper formalizes the $10$-step
macro-cycle operating over a $12$-channel lattice Hilbert space
($\Hilb_{\Lambda_{12}}$). By introducing dynamic transverse variables
($\pi^{2}$ and $\sqrt{\pi}$) as orthogonal heat sinks, formally postulating the
projection of Hilbert-space amplitudes into classical Markov probabilities, and
utilizing a topological ``Tesla-valve'' Markovian kernel, we rigorously prove
that the system breaks detailed balance. Utilizing explicit stochastic boundary
conditions, we demonstrate that the TTMPT generates a strictly positive,
continuous net-forward probability current, advancing the binding scalar field
while resetting the modular rotational phase to identity.
\end{abstract}

%================================================================= Section 1
\section{Axiomatic Foundations and State Space}

\subsection{The Rigorous State Space}
The structural bedrock of the system consists of a triple-trefoil skeleton. We
rigorously define the localized state space as a $12$-channel lattice Hilbert
space:
\[
  \Hilb_{\Lambda_{12}} \cong \mathbb{C}^{12}.
\]
The space operates using an explicit basis representing three coupled $4$-phase
streams (denoted by phase groups $\alpha,\beta,\gamma$). A state vector at
macro-step $n$ is defined as the superposition
\[
  \ket{\psi_n}
    = \sum_{g \in \{\alpha,\beta,\gamma\}} \sum_{r=1}^{4}
      c_{g,r}^{(n)}\, \ket{e_{g,r}}.
\]
This state vector is subject to the standard normalization constraint ensuring
conservation of total probability amplitude:
\[
  \sum_{g,r} \bigl\lvert c_{g,r}^{(n)} \bigr\rvert^{2} = 1.
\]

\subsection{Dynamical Transverse Variables}
To govern the continuous spatial scaling and continuous wave energy orthogonal
to the central longitudinal binding field $B$, we introduce a scalar
deformation parameter $\delta$ alongside continuous topological perturbation
modes $\hat{u}(t)$ and $\hat{v}(t)$. The transverse orthogonal foliation
variables are rigorously defined as
\begin{align*}
  u(t) &= \pi^{2} + \delta\,\hat{u}(t)
        && \text{(Outer Manifold Variance)},\\[2pt]
  v(t) &= \sqrt{\pi} + \delta\,\hat{v}(t)
        && \text{(Inner Skeleton Winding Parameter)}.
\end{align*}
These continuously parameterized variables dictate the continuous deformation of
the Clifford torus during the stochastic pump cycle. They explicitly define the
structural topological resistance function
$A_{ij}(t) = A_{ij}\bigl(u(t),v(t)\bigr) > 0$ for each graph edge, acting as
independent orthogonal heat sinks for reverse-propagating current.

%================================================================= Section 2
\section{Topological Asymmetry and Broken Detailed Balance}

\subsection{The Postulated Hilbert-to-Markov Probability Bridge}
To connect the quantum-like state vectors of $\Hilb_{\Lambda_{12}}$ to classical
stochastic dynamics, we explicitly define the projection mapping.

\begin{xdefinition}[Classical Markov Projection]
The \emph{classical Markov projection} of the Hilbert-space state is the
probability vector $p^{(n)}$ with entries
\[
  p_{g,r}^{(n)} = \bigl\lvert c_{g,r}^{(n)} \bigr\rvert^{2}.
\]
\end{xdefinition}

\noindent
Consolidating these probabilities yields a $12$-dimensional classical
probability vector $p^{(n)} = \bigl(p_1^{(n)},\dots,p_{12}^{(n)}\bigr)$. We
postulate that, under the coarse-grained pump dynamics, this projected
probability vector evolves according to a row-stochastic Markov kernel $K$ such
that
\[
  p^{(n+1)} = p^{(n)} K.
\]

\subsection{Broken Detailed Balance and the Asymmetric Kernel}
For a finite Markov chain with stationary distribution $\pi$, \emph{detailed
balance} holds if $\pi_i K_{ij} = \pi_j K_{ji}$ for all states $i,j$. To break
detailed balance, we define a non-empty oriented forward edge set
$\Efwd \neq \varnothing$.

\begin{lemma}[Asymmetric Markovian Kernel]\label{lem:asym}
Suppose that for every $(i,j)\in\Efwd$ the row-stochastic transition
probabilities satisfy
\[
  K_{ij} > 0, \qquad K_{ji} = K_{ij}\, e^{-\lambda A_{ij}},
\]
where $\lambda > 0$ and $A_{ij} > 0$. If, additionally, the stationary
distribution satisfies the structural condition
\[
  \pi_i > \pi_j\, e^{-\lambda A_{ij}},
\]
then it strictly follows that
\[
  \pi_i K_{ij} - \pi_j K_{ji} > 0.
\]
Hence the net probability current
$J = \sum_{(i,j)\in\Efwd}\bigl(\pi_i K_{ij} - \pi_j K_{ji}\bigr)$ over the
non-empty set is strictly positive.
\end{lemma}

\begin{proof}
The current along any directed edge $(i,j)$ is
$J_{ij} = \pi_i K_{ij} - \pi_j K_{ji}$. Substituting the topological suppression
rule $K_{ji} = K_{ij}\, e^{-\lambda A_{ij}}$ gives
\[
  J_{ij} = K_{ij}\bigl(\pi_i - \pi_j\, e^{-\lambda A_{ij}}\bigr).
\]
Because we have explicitly assumed $K_{ij} > 0$ and
$\pi_i > \pi_j\, e^{-\lambda A_{ij}}$, the term in parentheses is strictly
positive. Therefore $J_{ij} > 0$ for every edge in $\Efwd$. Since
$\Efwd \neq \varnothing$, there exists at least one edge $(i,j)$ such that
$\pi_i K_{ij} \neq \pi_j K_{ji}$, so detailed balance fails. Consequently the
net current $J$ over the non-empty set is a sum of strictly positive terms.
\end{proof}

%================================================================= Section 3
\section{Mechanics of the 10-Step Macro-Cycle}

\subsection{Phase I: Compression and the Frame 5 Solver}
Let the $12$-channel state lie in a Hilbert space $\Hilb \cong \mathbb{C}^{12}$,
and assume $\norm{\ket{\psi_0}} = 1$. Let $P_1,\dots,P_4$ be orthogonal
projections on $\Hilb$ and let $\Pi$ be a unitary discrete phase operator. Define
the compression operator
\[
  C = P_4 P_3 \Pi P_2 P_1.
\]
Since each $P_k$ is norm non-increasing and $\Pi$ is norm-preserving, $C$ is norm
non-increasing ($\norm{C\ket{\psi}} \le \norm{\ket{\psi}}$). Assuming
$C\ket{\psi_0} \neq 0$, define the normalized pre-pump compressed state by
\[
  \ket{\psi_{\mathrm{comp}}}
    = \frac{C\ket{\psi_0}}{\norm{C\ket{\psi_0}}}
    = \frac{P_4 P_3 \Pi P_2 P_1 \ket{\psi_0}}
           {\norm{P_4 P_3 \Pi P_2 P_1 \ket{\psi_0}}}.
\]
Then $\norm{\ket{\psi_{\mathrm{comp}}}} = 1$. The Frame 5 Algebraic Solver
provides the geometric phase-lock parameter
\[
  s = 1 - \frac{(Q_\alpha + Q_\beta - Q_\gamma)^{2}}{4\,Q_\alpha Q_\beta},
  \qquad Q_\alpha, Q_\beta > 0.
\]
Setting $s = \sin^{2}\!\left(\tfrac{2\pi}{3}\right) = \tfrac{3}{4}$ imposes the
symmetric $120^\circ$ phase-lock condition. This geometric condition motivates
the topology of the cycle, while the rigorous contraction mechanism is supplied
by the Markov kernel assumptions in the pump phase.

\subsection{Phase II: Core Pump Stroke and Time Parameterization}
During the pump interval $t \in [t_5, t_7]$, let $K(t)$ be a continuously
parameterized family of stochastic transition matrices. For example, in the
row-stochastic convention, $K_{ij}(t) \ge 0$ and $\sum_j K_{ij}(t) = 1$. The
compressed state induces a probability vector
\[
  p_i^{\mathrm{comp}} = \bigl\lvert \braket{e_i}{\psi_{\mathrm{comp}}} \bigr\rvert^{2}.
\]
The Markov kernel evolves this probability layer during the pump stroke. If the
larger theorem requires strict contraction, assume there exists
$0 \le \lambda < 1$ such that
$\norm{pK(t) - qK(t)}_1 \le \lambda\,\norm{p - q}_1$ for all admissible
probability vectors $p,q$ and all $t \in [t_5, t_7]$. Let $S_1, S_2, S_3$ be
unitary rotational operators ($S_k^{\ast} S_k = I$, so $S_k^{-1} = S_k^{\ast}$).
Assuming $\sum_{k=1}^{3}(S_k + S_k^{-1})\ket{\psi_0} \neq 0$, define the
normalized quaternion centroid by
\[
  \ket{\Psi_4}
    = \frac{\sum_{k=1}^{3}(S_k + S_k^{-1})\ket{\psi_0}}
           {\norm{\sum_{k=1}^{3}(S_k + S_k^{-1})\ket{\psi_0}}}.
\]
The active current $J(t)$ is then interpreted as a parameterized flow directed
toward $\ket{\Psi_4}$.

\subsection{Phase III: Double Toroidal Collapse}
Let $\bigl(u(t),v(t)\bigr) \in \mathbb{T}^{2}$ parameterize the toroidal
collapse, and let $A_{ij}\colon \mathbb{T}^{2} \to \mathbb{R}_{\ge 0}$ be
continuous topological resistance functions. Introduce a reverse-energy
functional $E_{\mathrm{rev}}(t) \ge 0$. The collapse phase is dissipative if
\[
  \frac{d}{dt}\,E_{\mathrm{rev}}(t)
    = -\sum_{i,j} A_{ij}\bigl(u(t),v(t)\bigr)\, R_{ij}(t)^{2} \le 0.
\]
It is strictly dissipative whenever at least one active resistance mode satisfies
$A_{ij}\bigl(u(t),v(t)\bigr)\, R_{ij}(t)^{2} > 0$. Thus reverse variance is
dissipated into transverse modes while the longitudinal field remains protected
by a separately imposed conservation or lower-bound condition.

\subsection{Phase IV: Encapsulation and Phase Reset}
Let $\Theta_k \in \mathbb{R}/2\pi\mathbb{Z}$ and define the modular phase update
by $\Theta_{k+1} = \Theta_k + \omega_k$. After one full $10$-step macro-cycle,
\[
  \Theta_{10} = \Theta_0 + \sum_{k=0}^{9} \omega_k.
\]
Encapsulation requires the residual modular rotation to vanish:
$\sum_{k=0}^{9} \omega_k \equiv 0 \pmod{2\pi}$. Equivalently,
$\Theta_{10} \equiv \Theta_0 \pmod{2\pi}$.

%================================================================= Section 4
\section{The Torsional Trefoil Markov Pump Theorem (TTMPT)}

With the axiomatic mechanics established, we formalize the principal theorem.

\begin{theorem}[TTMPT]\label{thm:ttmpt}
Let $\Lambda_{12}$ be a finite state space with $12$ states, and let
$\Efwd \subseteq \Lambda_{12} \times \Lambda_{12}$ be a non-empty oriented
forward edge set ($\Efwd \neq \varnothing$). Let $[t_5, t_7]$ be a
non-degenerate compact interval with $t_7 > t_5$. For each $t \in [t_5, t_7]$,
let $K(t)$ be a continuously parameterized family of discrete-time
row-stochastic, irreducible, aperiodic transition matrices. Assume that
$K_{ij}(t)$, the unique stationary distribution $\pi_i(t)$, and the topological
resistance function $A_{ij}(t) = A_{ij}\bigl(u(t),v(t)\bigr)$ are continuous in
$t$, satisfying
\[
  \pi(t) K(t) = \pi(t), \qquad \sum_i \pi_i(t) = 1, \qquad \pi_i(t) > 0.
\]
Assume that for every $(i,j) \in \Efwd$ and every $t \in [t_5, t_7]$,
\[
  K_{ij}(t) > 0, \qquad K_{ji}(t) = K_{ij}(t)\, e^{-\lambda A_{ij}(t)},
\]
where $\lambda > 0$ and $A_{ij}(t) > 0$, and assume
\[
  \pi_i(t) > \pi_j(t)\, e^{-\lambda A_{ij}(t)}.
\]
Define the continuous forward probability current by
\[
  J(t) = \sum_{(i,j) \in \Efwd}
         \bigl( \pi_i(t) K_{ij}(t) - \pi_j(t) K_{ji}(t) \bigr).
\]
Then $J(t) > 0$ for all $t \in [t_5, t_7]$. Consequently, if $B$ satisfies
$\dfrac{dB}{dt} = J(t)$, then
\[
  B(t_7) > B(t_5).
\]
Furthermore, if the discrete phase update is $\Theta_{k+1} = \Theta_k + \omega_k$
and satisfies the closure condition $\sum_{k=0}^{9} \omega_k \equiv 0 \pmod{2\pi}$,
then
\[
  \Theta_{10} \equiv \Theta_0 \pmod{2\pi}.
\]
\end{theorem}

\begin{proof}
For each $(i,j) \in \Efwd$ at any time $t \in [t_5, t_7]$, using the topological
suppression rule,
\begin{align*}
  \pi_i(t) K_{ij}(t) - \pi_j(t) K_{ji}(t)
    &= \pi_i(t) K_{ij}(t) - \pi_j(t) K_{ij}(t)\, e^{-\lambda A_{ij}(t)} \\
    &= K_{ij}(t)\bigl( \pi_i(t) - \pi_j(t)\, e^{-\lambda A_{ij}(t)} \bigr).
\end{align*}
Since $\pi_i(t) > \pi_j(t)\, e^{-\lambda A_{ij}(t)}$ by hypothesis, the quantity
in parentheses is strictly positive, and factoring out the strictly positive
$K_{ij}(t)$ yields
\[
  \pi_i(t) K_{ij}(t) - \pi_j(t) K_{ji}(t) > 0.
\]
Because $\Efwd$ is non-empty ($\Efwd \neq \varnothing$), there exists at least
one edge $(i,j)$ such that $\pi_i(t) K_{ij}(t) \neq \pi_j(t) K_{ji}(t)$, so
detailed balance fails at each $t \in [t_5, t_7]$. Summing over the forward edge
set gives a strictly positive continuous net current:
\[
  J(t) = \sum_{(i,j) \in \Efwd}
         \bigl( \pi_i(t) K_{ij}(t) - \pi_j(t) K_{ji}(t) \bigr) > 0.
\]
Given that the scalar field evolves as $\dfrac{dB}{dt} = J(t)$, we integrate over
the non-degenerate interval $[t_5, t_7]$:
\[
  B(t_7) - B(t_5) = \int_{t_5}^{t_7} J(t)\, dt.
\]
Because $J(t) > 0$ and is continuous throughout the entire interval, and
$t_7 > t_5$, the definite integral is strictly positive:
\[
  \int_{t_5}^{t_7} J(t)\, dt > 0.
\]
Hence $B(t_7) - B(t_5) > 0$, demonstrating $B(t_7) > B(t_5)$.

Finally, iterating the discrete phase updates through the $10$-step macro-cycle
gives
\[
  \Theta_{10} = \Theta_0 + \sum_{k=0}^{9} \omega_k.
\]
Using the phase-closure constraint $\sum_{k=0}^{9} \omega_k \equiv 0 \pmod{2\pi}$,
we obtain
\[
  \Theta_{10} \equiv \Theta_0 \pmod{2\pi}.
\]
Therefore, one full macro-cycle produces strictly positive advancement of the
scalar field $B$ while successfully resetting the modular phase to identity.
\end{proof}

\bigskip
\noindent\rule{\linewidth}{0.4pt}
\begin{center}\small\itshape End of Proof.\end{center}

\end{document}
